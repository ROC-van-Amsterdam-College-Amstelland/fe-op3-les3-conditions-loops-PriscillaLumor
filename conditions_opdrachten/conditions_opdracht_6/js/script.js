//javascript code

function Verm(){
    fst=parseInt(calc.fst.value);
    snd=parseInt(calc.snd.value);
    result=fst*snd;
    calc.result.value=result;
}

function Verd(){
    fst=parseInt(calc.fst.value);
    snd=parseInt(calc.snd.value);
    result=fst/snd;
    calc.result.value=result;
} 